package com.redhat.rest.client;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeSwiftResourceIT extends SwiftResourceTest {

    // Execute the same tests but in native mode.
}